<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.ckhd.opengame.app.dao.QQActivityDao">
	
	<insert id="insert">
		INSERT INTO t_qq_activity(
			id,
			imsi,
			ckAppId,
			sign,
			qq,
			create_date
		) VALUES (
			#{id},
			#{imsi},
			#{ckAppId},
			#{sign},
			#{qq},
			#{createDate}
		)
	</insert>
	
	<select id="getQQDataByTimeAndCkAppId" resultType="java.util.HashMap">
		SELECT qq FROM `t_qq_activity` a 
		WHERE a.`create_date` >= #{startTime} 
		AND a.`create_date` <![CDATA[<=]]> #{endTime}
		AND a.`ckAppId`=#{ckAppId}
	</select>
	
	<select id="existImsi" resultType="java.lang.Integer">
		select count(0) num
		from t_qq_activity a
		where a.imsi = #{imsi} and a.ckAppId=#{ckAppId}
	</select>
	
	<select id="existQQ" resultType="java.lang.Integer">
		select count(0) num
		from t_qq_activity a
		where a.qq = #{QQ} and a.ckAppId=#{ckAppId}
	</select>
	
</mapper>